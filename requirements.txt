psycopg2-binary   # used directly now by hierarchy/traverse.py (propose_hierarchy.py's --dry-run path doesn't need it)
python-dotenv
fastapi
pydantic
sqlalchemy
uvicorn
alembic
langchain-ollama
langchain-google-genai   # hierarchy/classify.py's Gemini path — local Ollama models don't fit this machine's 8GB RAM
pgvector   # SQLAlchemy Vector column type, for embeddings/model.py
bcrypt   # auth/security.py password hashing — used directly, not via passlib (bcrypt>=4.1 breaks passlib's version-sniffing)
pyjwt[crypto]   # auth/security.py JWT tokens — chosen over python-jose (simpler API, no algorithm-confusion CVE history)
reportlab   # services/invoice_pdf.py — pure-Python PDF generation, no system-level deps (unlike weasyprint)
