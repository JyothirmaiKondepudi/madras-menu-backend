psycopg2-binary   # used directly now by hierarchy/traverse.py (propose_hierarchy.py's --dry-run path doesn't need it)
python-dotenv
fastapi
pydantic
sqlalchemy
uvicorn
alembic
langchain-ollama
pgvector   # SQLAlchemy Vector column type, for embeddings/model.py

# Add one of these once a real LLM provider is wired into call_llm()
# in propose_hierarchy.py — see README.md "What's actually built":
# anthropic
# google-genai
# google-cloud-aiplatform
