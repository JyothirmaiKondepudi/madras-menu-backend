[pytest]
testpaths = tests
# Needed since the models/ package split: the plain `pytest` binary doesn't
# add the project root to sys.path the way `python -m pytest` does, so
# `import models` in conftest.py would fail without this.
pythonpath = .
